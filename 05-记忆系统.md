# 第 5 篇 · 记忆系统

> **搭建路线**：第 5 块积木——拼上真正的记忆：短期在上下文里，中期在任务草稿本里，长期在文件里。解决"任务足够长、上下文多次压缩后，重要事实从注意力中消失"的问题。
> **本篇目标**：掌握三层注意力架构、Workspace 笔记、记忆工具三原则；对照 DSH 的 goal/skills。
> **难度**：⭐⭐⭐

---

## 1. 为什么做了上下文引擎还需要记忆

上下文引擎解决"模型这一轮应该看到什么"（短期的、当前的）。但任务足够长、上下文经历多次压缩后，**某些出现过的事实会从模型注意力中消失**——上下文引擎管不了"跨轮次、跨会话"的事。这时需要记忆系统：让模型能主动把关键信息写入**非易失存储**，并在需要时取回。

## 2. 三层注意力架构

如果把所有事实都塞进 memory，模型很快就会混乱。正确做法是把信息分成三层（整合自 Tritium《Build An Agent From Scratch》[4]，CC BY-NC-SA）：

| 层 | 载体 | 生命周期 | 内容示例 |
|---|---|---|---|
| 短期记忆 | 当前上下文（上下文引擎管理） | 当前轮次 | 消息、工具结果、压缩摘要 |
| 中期记忆 | Workspace 笔记（任务草稿本） | 当前会话 | 阶段性结论、重要文件、错误记录、待办、设计决策 |
| 长期记忆 | Memory Store（Markdown 文件） | 跨任务跨会话 | 用户偏好、项目约定、可复用经验 |

### 中期记忆：Workspace 笔记

Workspace 笔记按**种类**管理，对应 Agent 在任务中最常需要留下来的东西：

```text
note     一般事实（"数据集是 CIFAR-10"）
decision 已做出的设计选择（"基线用 ResNet-18"）
file     重要文件或读过的入口（"入口: src/main.py"）
error    关键错误和失败原因（"GPU OOM: 显存 8G 不够 batch 64"）
todo     后续要做的事情（"待验证: batch 32 能否跑通"）
```

笔记跟随会话持久化——**history 被压缩了，笔记还在**。

### 长期记忆：Memory Store

跨任务、跨会话的 Markdown 文件（如 .agent-memory/MEMORY.md），保存用户偏好（"这个用户喜欢 TypeScript 示例"）、项目约定（"这个仓库 markdown 默认不纳入 git"）、可复用经验。模型通过工具读写，而不是宿主每轮自动注入。

## 3. 记忆工具的三条设计原则

1. **读写分离**：读取发生在任务各个阶段，应该轻量、可控、可解释；写入意味着决定"什么值得长期保存"，更危险，也更容易污染记忆库——两条路径分开设计；
2. **可解释性**：一次记忆召回，为什么命中、返回了什么，都要能明确归因——人能理解模型为什么会这么想（对调试和信任都重要）；
3. **不要喧宾夺主**：模型有 memory 不代表每轮都要看到所有 memory——过多旧偏好、旧经验会挤占短期上下文、拉走注意力。记忆是"按需召回"，不是"全量注入"。

## 4. 记忆与上下文引擎的分工

- Context Engine：决定**每轮**的内容视图（短期的、当前的）——历史与视图分离（第 4 篇）；
- Memory System：决定**长期**什么值得留、需要时怎么找回（中期的、跨会话的）；
- 记忆以**工具**的形式存在（读记忆/写记忆工具），由模型在需要时主动调用。

数据流（完整形态）：每轮构建运行时工具注册表（核心工具 + 记忆工具）→ 系统提示词含静态记忆指令 → 模型需要时调用读/写记忆工具 → 记忆内容以工具结果回填。

## 5. DSH 参照：目标、Skills 与持久会话

DSH 没有独立的"记忆系统"模块，但它把记忆拆进了三个机制（第 8 篇展开）：

| 三层记忆 | DSH 的对应 |
|---|---|
| 短期 | 会话上下文 + compaction（第 4 篇） |
| 中期 | `goal`（持久化长任务目标，跨轮次续跑）+ 持久会话（会话状态按 Session 键控） |
| 长期 | `skills`（可复用知识的 SKILL.md，按需加载——即"程序性记忆"） |

> 对照第 1 篇 §5 的记忆类型学：工作记忆（上下文）/长期记忆（向量库或文件）/程序性记忆（工具、skills、预设）——DSH 的 skills 机制就是"程序性记忆"的工程化：把"这类 bug 怎么修"写进 SKILL.md，按需加载。

## 6. 检验：读完本篇，你能回答吗

1. 三层注意力各存什么？生命周期分别是什么？
2. Workspace 笔记有哪五种？"GPU OOM"该记成哪种？
3. 记忆工具三条原则是什么？"读写分离"为什么重要？
4. 为什么记忆要"按需召回"而不是"全量注入"？
5. DSH 如何实现三层记忆？skills 对应哪层？

## 本章小结

第 5 篇拼上了记忆：重要事实可以跨轮次、跨会话留存。但记忆只是"记录"，复杂任务还需要"结构"——先做什么、后做什么、凭什么说完成了。下一篇拼上规划。

**下一篇**：[第 6 篇 · Planning](06-Planning.md) —— 计划从"文字"到"状态机"、审批闸门、Final Answer Guard。
