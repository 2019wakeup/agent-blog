# 第 4 篇 · 上下文引擎

> **搭建路线**：第 4 块积木——装上"大脑的仪表盘"：决定模型每一轮到底看到什么、哪些保留、哪些截断、哪些总结。
> **本篇目标**：理解"历史与视图分离"的核心设计；掌握六层流水线；实现一个最小的上下文引擎。
> **难度**：⭐⭐⭐⭐

---

## 1. 为什么需要上下文引擎

第 3 篇之后，Agent 开始接触真实环境：read_file 可能读到几千行源码，execute_command 可能吐出一整屏构建日志，fetch_url 可能抓回长网页。如果继续把完整 history 原样塞给模型，会遇到三个问题：

1. **上下文窗口爆炸**：即使工具层已做截断，"截断后的结果"仍会持续累积，迟早撑满窗口；
2. **注意力漂移**：模型看到的信息越多不一定越聪明——旧日志、旧错误会把它带偏，忘记当前真正要做什么；
3. **成本与缓存**：稳定内容应靠前、动态内容靠后，混成一条不断变长的消息数组会让缓存优化无从下手。

## 2. 核心设计：历史与视图分离

> **Agent 的完整消息历史（history）是事实记录，不能破坏；模型真正看到的（request-view）是工作视图，可以被截断、压缩、替换和重排。**

两个实体分离，压缩才敢放手做——这是整个上下文工程的根基，也是本章最重要的设计。

```text
完整 Agent history（事实记录，不可变）
        │
        ▼
Context Engine（生成"模型可见视图"）
        │
        ▼
request-view（工作视图，可截断/压缩/重排）→ LLM
```

## 3. 六层流水线

在 Agent Loop 把消息发给 LLM 之前，经过六道处理（整合自 Tritium《Build An Agent From Scratch》[3]，CC BY-NC-SA）：

| 层 | 职责 | 关键点 |
|---|---|---|
| ① Prompt Builder | 把稳定提示词拼成结构化前缀 | 按变化频率排序；环境背景渲染成 XML（含 cwd/日期/时区/shell），工具列表结构化——降低模型"解析成本" |
| ② Token 估算 | 让上下文有共同度量 | 不追求精确 tokenizer，用稳定低成本的经验公式近似（按字符数估算）——够用于预算决策 |
| ③ Budget Manager | 决定何时压缩 | 设定 token 预算线，超过才触发压缩，避免每轮都做昂贵操作 |
| ④ 启发式压缩 | 无损优先的常规手段 | 保留最近上下文（最近的工具结果最关键）；旧内容不能直接切除，要变成摘要 |
| ⑤ Handoff Summary | 旧历史变"交接记录" | 压缩不是丢记忆，是写交接班记录：把用户目标、已完成步骤、关键结论整理成 summary 传给下阶段 |
| ⑥ Dynamic Compression | 让模型自己触发折叠 | 把"压缩工具"暴露给模型：模型判断某段历史不再需要时主动调用折叠——比宿主单方面压缩更精准 |

## 4. 你的实现：最小上下文引擎（三个核心函数）

```python
# ① Token 估算：经验公式（够用于预算决策即可，不必精确）
def estimate_tokens(text: str) -> int:
    return len(text) // 2   # 中文约 1~2 字符/token；英文约 4 字符/token

# ② 预算决策：超过预算线才压缩
def budget_decision(messages, limit: int) -> bool:
    return sum(estimate_tokens(m["content"]) for m in messages) > limit

# ③ 启发式压缩 + Handoff：旧历史变摘要，保留最近上下文
def compact(messages, summarize, keep_recent: int = 8):
    old, recent = messages[:-keep_recent], messages[-keep_recent:]
    summary = summarize(old)   # 调模型总结：用户目标 + 已完成步骤 + 关键结论
    return [{"role": "system", "content": f"【历史交接】{summary}"}] + recent
```

三个函数对应六层中的 ②③④⑤。①（Prompt Builder）的核心是"稳定前缀分层"（第 1 篇 §6 的缓存友好分层表直接落地）；⑥（Dynamic Compression）需要把压缩做成工具交给模型——你的循环已经有工具系统了，加一个 `compact_history` 工具即可。

## 5. DSH 参照：生产级的压缩与修剪

DSH 的上下文管理（第 8 篇展开）与六层流水线的对应：

| 六层 | DSH 的对应实现 |
|---|---|
| ④ 启发式压缩 | `dsh-compaction-basic`：上下文接近上限时把旧对话总结成摘要替换 |
| ④ 修剪 | `dsh-compaction-tool-result-pruner`：`thresholdChars: 8192`（超过才修剪）、`headChars: 4096` + `tailChars: 1024`——**不对称修剪**：头部通常含结构、尾部含结论，中间最不重要 |
| ② 计量 | `dsh-token-meter`：浏览器侧显示每个会话的上下文用量 |
| ① 缓存友好 | PTC 的 SDK 生成"字典序、字节级确定"（第 8 篇）——提示词前缀稳定到极致 |

> 注意到修剪参数的不对称了吗？head 4096 + tail 1024——这是对 LLM 阅读习惯的工程适配，也是"最小信息原则"（第 1 篇 §7）的具体落地。

## 6. 检验：读完本篇，你能回答吗

1. "历史与视图分离"是什么意思？为什么分离了压缩才敢放手做？
2. 六层流水线各管什么？哪一层负责"何时压缩"？哪一层把旧历史变交接记录？
3. Token 估算为什么不追求精确？够用于什么决策？
4. DSH 的修剪参数为什么不对称（head 4096 + tail 1024）？
5. Dynamic Compression 与宿主单方面压缩的区别是什么？

## 本章小结

第 4 篇拼上了"大脑的仪表盘"：模型每轮看到什么由流水线决定，历史是事实记录、视图是工作视图。但压缩再聪明，也只是"短期管理"——跨轮次、跨会话的重要事实怎么办？下一篇拼上真正的记忆。

**下一篇**：[第 5 篇 · 记忆系统](05-记忆系统.md) —— 三层注意力、Workspace 笔记、记忆工具三原则。
