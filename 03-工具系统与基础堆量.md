# 第 3 篇 · 工具系统与基础堆量

> **搭建路线**：第 3 块积木——给 Agent 装上"手脚"：文件、Shell、Web 工具，以及让工具稳定工作的工程规范。没有这一步，后续的上下文引擎、记忆、Planning 都无东西可管。
> **本篇目标**：掌握工具契约的三层设计；工具工程的边界规范；Provider 边界。
> **难度**：⭐⭐⭐

---

## 1. 为什么先"堆量"再"管理"

第 2 篇的 Agent 只有 calculator 和 get_weather 两个教学工具，工具结果只有几行文本，几乎不会制造上下文压力。但真实世界的工具输出动辄几千行：源码（read_file）、构建日志（execute_command）、网页正文（fetch_url）、错误堆栈——**先让 Agent 的手脚接触真实世界，上下文管理才有存在的意义**。

所以本篇的顺序是：工具契约 → 工具工程规范 → Provider 边界 → 截断（第一道防线）。

## 2. 工具契约的三层设计

工具是 Agent 与世界的唯一接口，所以**契约（模型看到的 schema + 返回结构）比实现更重要**。以最常用的 read_file 为例：

```ts
// 工具注册（以 DSH 的 ctx.tools.register 形态为例）
ctx.tools.register({
  name: 'read_file',
  description: '读取文本文件。大文件请用 offset/limit 分页读取。',  // ← 模型靠它决策
  parameters: {                        // 契约层 1：模型能传什么
    type: 'object', additionalProperties: false,
    properties: {
      path: { type: 'string' },
      offset: { type: 'integer', description: '起始行（0 基），大文件分页用' },
      limit: { type: 'integer', default: 200, description: '最多返回行数' },
    },
    required: ['path'],
  },
  output: {                            // 契约层 2：模型能拿到什么（规范输出）
    schema: { type: 'object', additionalProperties: false,
      properties: {
        content: { type: 'string', required: true },
        truncated: { type: 'boolean', required: true },   // 明确告知截断
        totalLines: { type: 'integer', required: true },
      } },
  },
  async execute(args) { return readFileWithOffset(args) },   // 实现层：内部细节
})
```

**为什么三层设计**：契约层 1 决定"模型会不会乱传参"（additionalProperties: false + 描述）；契约层 2 决定"下游（前端/评测/CI）能否类型安全地消费"；实现层可以随时重写而不影响任何调用方。

**错误契约**同样重要：失败时 `throw new Error("人类可读的原因")`——模型会读到错误消息并自我纠正；静默吞错是最差实践（第 2 篇 §1 第 3 条）。

## 3. 工具工程规范：真实世界的四件套

生产工具的标配（整合自 Tritium《Build An Agent From Scratch》[2.5]，CC BY-NC-SA）：

1. **读文件支持 offset/limit 分页**，并在描述里提醒模型"大文件要继续用 offset 读"——让 Agent 自己管理读取粒度；
2. **Shell 工具强制 timeout**，超时**杀整个进程树**（不能只杀父进程，子进程会残留），stdout/stderr 流式累积；
3. **所有工具输出做截断（truncation）**——这是上下文的第一道防线，在上下文引擎（第 4 篇）之前就控制体积；截断必须**显式标记**（truncated: true），不能静默丢数据；
4. **描述即文档**：工具描述写清"什么时候用、参数什么含义、大输出的处理方式"——描述是模型对工具唯一的认知来源。

```ts
// Shell 工具的安全三件套（对应 AutoResearcher 工程的 run_experiment）
// 铁律 1：路径必须解析后仍落在白名单根内（防 ../ 逃逸）
if (relative(SCRIPTS_ROOT, full).startsWith('..')) throw new Error('脚本必须在白名单目录下')
// 铁律 2：强制超时 + SIGKILL 进程树（防失控）
const timer = setTimeout(() => child.kill('SIGKILL'), timeoutSec * 1000)
// 铁律 3：日志全量落盘（审计 + 可复现）
writeFileSync(logPath, Buffer.concat(chunks).toString('utf8'))
```

## 4. Provider 边界：模型是插拔的，循环不是

多模型支持的正确形态（[2.5] 篇）：**Agent Loop 只依赖一个统一的 LLM Client 接口**；OpenAI Responses / Chat Completions / Anthropic 等具体适配各自实现该接口，独立承载鉴权、重试、超时、流式。

```ts
export interface LlmClient {
  complete(request: LlmRequest): Promise<AssistantMessage>
}
export interface StreamingLlmClient extends LlmClient {
  stream(request: LlmRequest): AsyncIterable<LlmEvent>
}
// Loop 只面对接口；Provider 适配在边界层，可插拔
```

> 工程意义：模型供应商升级/切换时，只改适配层，循环与工具完全不动——这也是第 9 篇 AutoResearcher 选择"模型路由由宿主管理"的原因。

## 5. DSH 参照：注册表是工具系统的生产形态

你在 §2 手写的注册逻辑，DSH 的生产形态是**工具注册表**（dsh-tools，第 8 篇展开）：

- `ctx.tools.register(definition)`：注册必须带**规范输出契约**（output schema），缺失即注册失败——"契约是强制"而不是"建议"；
- `tools.restrict(filter)`：agent 作用域的 allow/deny 掩码——**可见性控制是工具系统的一部分**；
- 工具与 Provider 一样可插拔：预设决定注册哪些工具（第 8 篇的 agent.cordis.yml），"没有工具 = 做不了"是最强的范围控制。

> 对照你自己的实现：你手动维护 TOOLS 数组；生产级是注册表 + 契约强制 + 作用域可见性控制。

## 6. 检验：读完本篇，你能回答吗

1. 工具契约的三层设计是什么？为什么 output 契约"缺失即注册失败"？
2. 真实世界工具的四件套规范是什么？截断为什么必须显式标记？
3. Shell 工具的三条安全铁律是什么？各防什么事故？
4. Provider 边界的意义是什么？切换模型时哪里要动、哪里不动？
5. 为什么说"没有工具 = 做不了"是最强控制？

## 本章小结

第 3 篇给 Agent 装上了手脚：文件可读、命令可跑、网页可搜，且有契约、安全、边界规范兜底。但手脚一多，上下文压力就来了——工具输出、日志、错误开始累积。下一篇拼上"大脑的仪表盘"：上下文引擎。

**下一篇**：[第 4 篇 · 上下文引擎](04-上下文引擎.md) —— 历史与视图分离、六层流水线、KV 缓存工程化。
