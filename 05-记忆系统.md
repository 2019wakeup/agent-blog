# 第 5 篇 · 记忆系统

> **搭建路线**：第 5 块积木——拼上真正的记忆：短期在上下文里，中期在任务草稿本里，长期在文件里。解决"任务足够长、上下文多次压缩后，重要事实从注意力中消失"的问题。
> **本篇目标**：掌握三层注意力架构、Workspace 五种笔记、记忆工具三原则（每个都有展开与例子）；理解记忆与上下文引擎的分工。
> **难度**：⭐⭐⭐

---

## 1. 为什么做了上下文引擎还需要记忆

设想一个真实长任务："阅读仓库最近一轮变更，对照前四章文章，写出完整第五章并落盘。" 任务进行到第 20 轮，上下文经历了两次压缩——压缩后的摘要里，你最初记下的"第五章应承接哪个问题"可能已经模糊。

**上下文引擎管不了"跨轮次、跨会话"的事**——它只决定每轮的视图。记忆系统解决三个问题：

1. 哪些事实值得从短期上下文里**提取**出来？
2. 这些事实应该存在当前任务里，还是长期保存？（**分层**）
3. 模型需要时，怎样把这些事实**取回**？

答案（整合自 Tritium《Build An Agent From Scratch》[4]，CC BY-NC-SA）：三层注意力 + 记忆工具 + 静态记忆指令。

## 2. 三层注意力架构

如果把所有事实都塞进 memory，模型很快就会混乱。正确做法是把信息分成三层：

| 层 | 载体 | 生命周期 | 内容示例 |
|---|---|---|---|
| 短期记忆 | 当前上下文（上下文引擎管理） | 当前轮次 | 消息、工具结果、压缩摘要 |
| 中期记忆 | Workspace 笔记（任务草稿本） | 当前会话 | 阶段性结论、重要文件、错误记录、待办、设计决策 |
| 长期记忆 | Memory Store（Markdown 文件） | 跨任务跨会话 | 用户偏好、项目约定、可复用经验 |

### 2.1 中期记忆：Workspace 笔记

Workspace 笔记按**种类**管理，对应 Agent 在任务中最常需要留下来的东西：

```text
note     一般事实          → "数据集是 CIFAR-10，输入尺寸 32x32"
decision 已做出的设计选择  → "基线模型用 ResNet-18，不用 VGG"
file     重要文件或入口    → "入口: src/main.py；配置: config.py:45"
error    关键错误与原因    → "GPU OOM: 8G 显存不够 batch=64，需降到 32"
todo     后续要做的事情    → "待验证: batch=32 能否跑通；待对比: 两个学习率"
```

**为什么按种类分**：模型召回时按需过滤（"有哪些待办？"→ kind=todo），而不是把整本笔记塞回去。笔记跟随会话持久化——**history 被压缩了，笔记还在**。

### 2.2 长期记忆：Memory Store

跨任务、跨会话的 Markdown 文件（如 .agent-memory/MEMORY.md），保存：

```text
## 用户偏好
- 喜欢 TypeScript 示例，不要 Java
- 文章用中文写，代码注释用英文

## 项目约定
- 这个仓库的 markdown 成稿默认不纳入 git
- 构建产物放 dist/，测试夹具放 fixtures/

## 可复用经验
- 这个项目的 API 限流是 429 + Retry-After，失败时等待后重试
```

模型通过工具读写，而不是宿主每轮自动注入。

## 3. 记忆工具的三条设计原则

### 3.1 读写分离

读取发生在任务各个阶段，应该**轻量、可控、可解释**（读工具可以被模型随时调用）；写入意味着决定"什么值得长期保存"，**更危险**（写错了会污染记忆库，误导后续所有任务）。两条路径分开设计：

```text
读：memory_read（按 kind/关键词过滤，返回少量命中）
写：memory_write（需要明确"为什么值得保存"，可设审批）
```

### 3.2 可解释性

一次记忆召回，为什么命中、返回了什么，都要能明确归因——人能理解模型为什么会这么想。对调试（"它怎么知道这个约定的？"）和信任（企业审计）都重要。实现上：每次召回返回来源路径与匹配原因。

### 3.3 不要喧宾夺主

模型有 memory 不代表每轮都要看到所有 memory——过多旧偏好、旧经验会挤占短期上下文、拉走注意力（第 1 篇定律三）。记忆是"**按需召回**"，不是"全量注入"：模型判断需要时才调用读记忆工具。

## 4. 记忆与上下文引擎的分工（数据流全景）

- **Context Engine**（第 4 篇）：决定**每轮**的内容视图——短期的、当前的；
- **Memory System**（本篇）：决定**长期**什么值得留、需要时怎么找回——中期的、跨会话的；
- 记忆以**工具**的形式存在（读记忆/写记忆工具），由模型主动调用。

完整数据流（生产形态）：

```text
user input
  → append user message to history
  → 构建运行时工具注册表（核心工具 + 记忆工具 + 压缩工具）
  → 构建 system prompt（默认指令 + 环境背景 + 静态记忆指令）
  → ContextEngine.prepareWithHandoff() → request view
  → LLM → 回答或工具调用
  → 执行工具（记忆内容以 tool result 回填）
  → 追加消息到 history
  → 会话持久化（history + Workspace 笔记 + 用量）
```

> 注意"静态记忆指令"：system prompt 里只告诉模型"你有读/写记忆的工具，长任务记得定期把重要结论写入 Workspace"——**指令是静态的（缓存友好），内容是动态的（按需召回）**。这正是第 1 篇 §6 缓存友好分层的实践。

## 5. DSH 参照：目标、Skills 与持久会话

DSH 没有独立的"记忆系统"模块，但它把记忆拆进了三个机制（第 8 篇展开）：

| 三层记忆 | DSH 的对应 |
|---|---|
| 短期 | 会话上下文 + compaction（第 4 篇） |
| 中期 | `goal`（持久化长任务目标，跨轮次续跑）+ 持久会话（会话状态按 Session 键控） |
| 长期 | `skills`（可复用知识的 SKILL.md，按需加载——即"程序性记忆"） |

> 对照第 1 篇 §5 的记忆类型学：工作记忆（上下文）/长期记忆（向量库或文件）/程序性记忆（工具、skills、预设）——DSH 的 skills 机制就是"程序性记忆"的工程化：把"这类 bug 怎么修"写进 SKILL.md，按需加载（第 7 篇展开）。

## 6. 练习与常见坑

> **练习 5.1**：给第 2 篇的循环加两个工具（memory_write / memory_read，实现为本地 JSON 文件），跑一个"分两轮会话完成的任务"——第一轮写入关键结论，第二轮从文件恢复。
> **练习 5.2**：故意让模型写 50 条笔记，观察上下文膨胀——体会"不要喧宾夺主"原则为什么存在。

**常见坑**：
1. **每轮自动注入全部记忆**：上下文被旧经验塞满，当前任务注意力被拉走；
2. **写记忆不设门槛**：什么垃圾都存，记忆库变成噪声池（读写分离 + 审批）；
3. **召回不可解释**：模型用了某条记忆但说不清来源——无法调试与审计；
4. **把记忆当成上下文引擎的替代品**：两者分工不同（每轮视图 vs 长期留存），缺一不可。

## 7. 检验：读完本篇，你能回答吗

1. 三层注意力各存什么？生命周期分别是什么？
2. Workspace 笔记有哪五种？"GPU OOM"该记成哪种？"入口在 src/main.py"呢？
3. 记忆工具三条原则是什么？"读写分离"为什么重要？
4. 为什么记忆要"按需召回"而不是"全量注入"？（物理原因是什么）
5. "静态记忆指令"是什么意思？它如何兼顾缓存友好？
6. DSH 如何实现三层记忆？skills 对应哪层？

（答案都在上文：1-2→§2，3→§3，4→§3.3，5→§4，6→§5）

## 本章小结

第 5 篇拼上了记忆：重要事实可以跨轮次、跨会话留存。但记忆只是"记录"，复杂任务还需要"结构"——先做什么、后做什么、凭什么说完成了。下一篇拼上规划。

**下一篇**：[第 6 篇 · Planning](06-Planning.md) —— 计划从"文字"到"状态机"、审批闸门、Final Answer Guard。
