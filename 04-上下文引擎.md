# 第 4 篇 · 上下文引擎

> **搭建路线**：第 4 块积木——装上"大脑的仪表盘"：决定模型每一轮到底看到什么、哪些保留、哪些截断、哪些总结。
> **本篇目标**：理解"历史与视图分离"的核心设计；掌握六层流水线（每层都有实现要点）；实现一个最小但完整的上下文引擎。
> **难度**：⭐⭐⭐⭐

---

## 1. 为什么需要上下文引擎：三个问题

第 3 篇之后，Agent 开始接触真实环境。如果继续把完整 history 原样塞给模型，会遇到三个问题（每一个都是可量化的）：

**问题一：上下文窗口爆炸。** 一次 read_file 返回 200 行 × 多次调用 + 构建日志 + 错误堆栈 + 历史对话——假设每轮新增 2000 token，50 轮任务就是 100K token，已经接近主流模型上限。即使工具层已做截断，"截断后的结果"仍会持续累积。

**问题二：注意力漂移。** 模型看到的信息越多不一定越聪明——注意力总量固定（第 1 篇定律三），旧日志、旧错误会把它带偏，忘记当前真正要做什么。

**问题三：成本与缓存。** 稳定内容应靠前、动态内容靠后（第 1 篇 §6），混成一条不断变长的消息数组会让缓存优化无从下手——每轮全量计费。

## 2. 核心设计：历史与视图分离

> **Agent 的完整消息历史（history）是事实记录，不能破坏；模型真正看到的（request-view）是工作视图，可以被截断、压缩、替换和重排。**

两个实体分离，压缩才敢放手做——这是整个上下文工程的根基，也是本章最重要的设计。代码形态：

```ts
// Agent 自己的事实记录（只追加，永不修改）
this.messages.push(userMessage)
this.messages.push(assistantMessage)
this.messages.push(toolResult)      // ← 事实记录：history

// 发给模型的工作视图（可截断/压缩/重排）
const request: LlmRequest = {
  systemPrompt: this.buildSystemPrompt(),   // ← 上下文引擎生成
  messages: this.contextEngine.prepare(this.messages),  // ← 视图
  tools: this.tools.toLlmToolSpecs(),
}
```

```text
完整 Agent history（事实记录，不可变）
        │
        ▼
Context Engine（生成"模型可见视图"）
        │
        ▼
request-view（工作视图，可截断/压缩/重排）→ LLM
```

> 为什么不能直接在 history 上压缩？因为压缩是有损的——如果压缩后任务失败需要重放，或用户要查看原始记录，被破坏的 history 无法恢复。**事实记录与工作视图分离 = 可逆的优化**。

## 3. 六层流水线（每层详解）

在 Agent Loop 把消息发给 LLM 之前，经过六道处理（整合自 Tritium《Build An Agent From Scratch》[3]，CC BY-NC-SA）：

### ① Prompt Builder：稳定提示词的结构化前缀

不只是"你是一个助手"，还包括：默认行为规则、当前工作目录/日期/时区/shell、可用工具列表、用户目标。**按变化频率排序拼接**（第 1 篇 §6 的缓存友好分层直接落地），环境背景渲染成结构化 XML：

```xml
<environment_context>
  <cwd>/path/to/project</cwd>
  <current_date>2026-08-15</current_date>
  <timezone>Asia/Shanghai</timezone>
  <shell>zsh</shell>
</environment_context>
<available_tools>
  <tool name="read_file">Read a UTF-8 file.</tool>
</available_tools>
```

> 为什么 XML：显式结构化隔离（第 1 篇 §7 原则 2）——降低模型"解析成本"，把注意力留给推理。

### ② Token 估算：让上下文有共同度量

不追求精确 tokenizer（不同模型 tokenizer 不同，引入精确估算依赖成本高），用稳定低成本的经验公式近似：

```python
def estimate_tokens(text: str) -> int:
    # 经验公式：中文约 1~2 字符/token，英文约 4 字符/token
    # 混合文本取 len/2 是够用的近似——预算决策不需要精确到个位
    return len(text) // 2
```

### ③ Budget Manager：决定何时压缩

设定 token 预算线（如窗口的 80%），超过才触发压缩——避免每轮都做昂贵的摘要操作：

```python
def budget_decision(messages, limit: int) -> bool:
    used = sum(estimate_tokens(m.get("content", "")) for m in messages)
    return used > limit          # True = 需要压缩
```

### ④ 启发式压缩：无损优先的常规手段

保留最近上下文（最近的工具结果最关键——它指导下一步动作）；旧内容不能直接切除，要变成摘要：

```python
def compact(messages, summarize, keep_recent: int = 8):
    old, recent = messages[:-keep_recent], messages[-keep_recent:]
    summary = summarize(old)   # 调模型总结（见⑤）
    return [{"role": "system", "content": f"【历史交接】{summary}"}] + recent
```

### ⑤ Handoff Summary：旧历史变"交接记录"

压缩不是丢记忆，是写交接班记录——把**用户目标、已完成步骤、关键结论**整理成 summary 传给下阶段。这是"状态锚定"（第 1 篇 §5.2 机制 3）的落地：模型压缩后依然知道自己在干什么。

```text
【历史交接】
- 用户目标：修复 src/main.py 的登录超时
- 已完成：定位到 session 模块第 120 行；确认超时配置在 config.py:45
- 关键结论：timeout=30 秒过短；已读取相关代码 3 处
- 待办：修改配置 + 验证
```

### ⑥ Dynamic Compression：让模型自己触发折叠

把"压缩工具"暴露给模型：模型判断某段历史不再需要时主动调用折叠——**比宿主单方面压缩更精准**（宿主不知道哪些内容模型已经不需要了）。你的循环已经有工具系统（第 3 篇），加一个 `compact_history` 工具即可：

```ts
ctx.tools.register({
  name: 'compact_history',
  description: '折叠一段不再需要的对话历史为摘要，释放上下文空间。当任务步骤已完成、旧内容不再需要时调用。',
  parameters: { type: 'object', additionalProperties: false,
    properties: { since: { type: 'string', description: '从哪条消息开始折叠' } } },
  async execute(args) { return this.dynamicCompress(args.since) },
})
```

## 4. 六层与 KV 缓存的关系

第 1 篇 §6 的缓存友好分层不是独立话题——它就是六层流水线的设计约束：

- ① Prompt Builder 输出的**稳定前缀**（system + 工具列表 + 环境）就是缓存命中的区间；
- ④⑤ 压缩只影响**中间/尾部**（历史区），不碰前缀——压缩前后缀不变，缓存不失效；
- ⑥ Dynamic Compression 同理：折叠的是历史区。

> 结论：**六层流水线的"层间变化频率单调递增"不是巧合，是缓存经济的必然**。

## 5. DSH 参照：生产级的压缩与修剪

DSH 的上下文管理（第 8 篇展开）与六层流水线的对应：

| 六层 | DSH 的对应实现 |
|---|---|
| ④ 启发式压缩 | `dsh-compaction-basic`：上下文接近上限时把旧对话总结成摘要替换 |
| ④ 修剪 | `dsh-compaction-tool-result-pruner`：`thresholdChars: 8192`（超过才修剪）、`headChars: 4096` + `tailChars: 1024`——**不对称修剪**：头部通常含结构、尾部含结论，中间最不重要 |
| ② 计量 | `dsh-token-meter`：浏览器侧显示每个会话的上下文用量 |
| ① 缓存友好 | PTC 的 SDK 生成"字典序、字节级确定"（第 8 篇）——提示词前缀稳定到极致 |

> 注意到修剪参数的不对称了吗？head 4096 + tail 1024——这是对 LLM 阅读习惯的工程适配，也是"最小信息原则"（第 1 篇 §7）的具体落地。

## 6. 练习与常见坑

> **练习 4.1**：给第 2 篇的循环接入最小上下文引擎（§3 的三个函数），跑一个"连续读 10 个文件"的任务，观察压缩何时触发、触发后模型是否还记得任务目标（Handoff Summary 的作用）。
> **练习 4.2**：对比修剪的对称（head 2500+tail 2500）与不对称（head 4096+tail 1024）对模型理解的影响——用一段"开头是结构、中间是细节、结尾是结论"的长文本测。

**常见坑**：
1. **在 history 上直接压缩**：事实记录被破坏，出错无法重放（必须视图分离）；
2. **每轮都压缩**：昂贵且没必要——用 Budget Manager 设阈值；
3. **压缩后模型"失忆"**：summary 没写用户目标与当前状态（Handoff Summary 的职责）；
4. **Token 估算精确癖**：追求精确 tokenizer 而忽略"够用即可"——预算决策不需要个位数精度。

## 7. 检验：读完本篇，你能回答吗

1. "历史与视图分离"是什么意思？为什么分离了压缩才敢放手做？（提示：可逆性）
2. 六层流水线各管什么？哪层负责"何时压缩"？哪层把旧历史变交接记录？
3. Handoff Summary 必须包含哪三样？（用户目标/已完成/关键结论）
4. Token 估算为什么不追求精确？够用于什么决策？
5. 六层与 KV 缓存的关系是什么？为什么压缩不碰前缀？
6. DSH 的修剪参数为什么不对称（head 4096 + tail 1024）？
7. Dynamic Compression 与宿主单方面压缩的区别是什么？

（答案都在上文：1→§2，2→§3，3→§3⑤，4→§3②，5→§4，6→§5，7→§3⑥）

## 本章小结

第 4 篇拼上了"大脑的仪表盘"：模型每轮看到什么由流水线决定，历史是事实记录、视图是工作视图。但压缩再聪明，也只是"短期管理"——跨轮次、跨会话的重要事实怎么办？下一篇拼上真正的记忆。

**下一篇**：[第 5 篇 · 记忆系统](05-记忆系统.md) —— 三层注意力、Workspace 笔记、记忆工具三原则。
