# 第 10 篇 · 前沿与学习路线

> **搭建路线**：图纸看完、积木拼完、产品交付——最后一篇看向前方：MCP 深入、多 Agent 编排、Agent 评测前沿，以及本科生 3 个月进阶路线图。
> **本篇目标**：打开视野 + 拿到路线图。读完你就知道"下一步学什么"。

---

## 1. MCP 深入：从"会用"到"会写 Server"

### 1.1 一个 MCP 会话的完整生命周期

```text
1. initialize 握手       客户端声明能力，Server 声明协议版本
2. notifications/initialized  Server 确认
3. tools/list            客户端发现工具（支持增量更新）
4. tools/call            调用工具（每次一个 JSON-RPC 请求）
5. 资源同步              resources/list → subscribe → 变更通知
6. 关闭                  客户端退出
```

### 1.2 三种传输的取舍

| 传输 | 适合 | 特点 |
|---|---|---|
| stdio | 本地子进程 | 最简单，进程级隔离 |
| HTTP + SSE | 远程服务 | 传统流式 |
| Streamable HTTP | 远程服务（2025+ 主流） | 单一端点双向流，云部署友好 |

### 1.3 安全边界（第 7 篇回顾 + 深化）

MCP Server 的能力 = 你授予 Agent 的能力。审查清单：只接可信来源；Server 用最小权限账号；敏感数据 Server 侧脱敏；所有调用留审计。

## 2. 多 Agent 编排：从"单 Agent"到"团队"

单 Agent 有天花板（上下文有限、视角单一、容易陷入局部最优）。三种经典模式：

| 模式 | 结构 | 适用 | 风险 |
|---|---|---|---|
| Orchestrator-Worker（总-分） | 一个调度者分解任务，worker 并行执行 | 大批量独立子任务 | 调度者成瓶颈 |
| Handoff（转交） | 按技能转交（第 7 篇框架横评的 OpenAI Agents SDK） | 领域分明的流水线 | 转交链过长 |
| Debate（辩论） | 互相质询 | 提高决策质量 | token 成本爆炸 |

**成本铁律**：多 Agent 的 token 成本线性叠加。先单 Agent 跑通，按 ROI 加 Agent；每次加 Agent 都过评测。实用升级路径：单 Agent（基线）→ 双 Agent（发现者 + 复核者，砍误报）→ 按需加 Worker（批量并行）。

> 第 9 篇的 AutoResearcher 工程里就有双评审实现（scripts/double-review.mjs：发现者 + 复核者两个隔离会话 + 合并规则——误报降级 info、漏报补充 warning）。

**DSH 的 Workflow 脚本化编排**（多 Agent 的工程形态，可运行概念示例）：

```js
// 场景：多角度评审一份代码改动——3 个独立 worker 并行 + 1 个汇总
const results = await parallel([
  () => agent('以「安全」视角评审这份 diff，返回结构化发现'),
  () => agent('以「性能」视角评审这份 diff，返回结构化发现'),
  () => agent('以「可维护性」视角评审这份 diff，返回结构化发现'),
])
const report = await agent('汇总以下三份评审，去重合并，按严重度排序：' + JSON.stringify(results))
return report
```

> 与第 6 篇 Planner 的关系：Workflow 是"脚本化的确定性编排"（适合可预知的流水线）；Planner 是"模型自主的状态机"（适合不可预知的长任务）。生产系统两者都用。

## 3. Agent 评测前沿：从基准到生产门禁

### 3.1 学术界基准

| 基准 | 测什么 |
|---|---|
| SWE-bench / Verified | 修真实 GitHub issue 并过测试（编码 Agent 事实标准） |
| Terminal-Bench | 终端操作类任务 |
| OSWorld / WebArena | 桌面/网页交互 |

### 3.2 生产实践的五个共识（比基准更重要）

1. **领域评测集 > 通用基准**：把团队真实历史 MR/issue 沉淀成评测集（第 9 篇 §7 的做法）；
2. **可复现三件套**：固定模型版本、温度=0、评测集版本化；
3. **双层结构**：工具单测管正确性，E2E 评测管任务完成度；
4. **监控"悄悄变笨"**：模型供应商升级后必须先跑回归再放量；
5. **成本也是指标**：通过率 × token 成本一起看。

**一个最小的领域评测集骨架**（manifest 驱动，对应第 9 篇 §7 的落地）：

```jsonc
{
  "schemaVersion": 1,
  "cases": [
    {
      "id": "sql-injection-in-query",
      "task": "审查 fixtures/sql-injection.diff，输出分级报告",
      "fixtures": ["fixtures/sql-injection.diff"],
      "graders": [
        { "type": "json-path", "path": "$.issues[?(@.severity=='critical')]", "mustExist": true },
        { "type": "regex", "pattern": "sql|injection", "caseInsensitive": true }
      ],
      "budget": { "maxInputTokens": 20000, "maxSteps": 10 }
    }
  ]
}
```

## 4. 其他值得跟的前沿

- **上下文工程（Context Engineering）**：从"塞更多"到"精准投喂"——检索、摘要、KV 缓存友好的提示词组织（第 4 篇的六层流水线就是它的工程形态）；
- **Agent 安全**：提示词注入是头号威胁；防御 = 工具最小权限 + 输出过滤 + 内容隔离（第 9 篇的沙箱+审批）；
- **工具生态**：MCP Registry 收录数千 Server，"应用商店"效应显现；
- **模型侧演进**：1M 上下文、更强代码推理、原生工具调用质量——模型变强会改变框架取舍（如 PTC 收益随模型编程能力提升而变大）；
- **Agent 记忆**：长期记忆从 RAG 走向"结构化记忆层"（entity/event 抽取）。

## 5. 本科生 3 个月进阶路线图

### 第 1 个月：打牢基础（每周 ~10 小时）

| 周 | 内容 | 产出 |
|---|---|---|
| 1 | 本系列第 1-2 篇 + 练习：跑通 35 行循环 | 可对话的 mini Agent |
| 2 | 第 3-4 篇：给 mini Agent 加工具 + 最小上下文引擎 | 有手脚的 Agent |
| 3 | 第 5-6 篇：加记忆与 Planner | 能完成多步任务的 Agent |
| 4 | 第 7-8 篇：接 MCP + 读懂 DSH 生产级参照 | 能看懂生产代码 |

### 第 2 个月：企业级实战

| 周 | 内容 | 产出 |
|---|---|---|
| 5 | 第 9 篇 §1-4：AutoResearcher 立项 + 预设 + 工具 | 自定义预设跑通 |
| 6 | 第 9 篇 §5-6：安全治理 + 成本计量 | 审计日志可查询 |
| 7 | 第 9 篇 §7-8：评测集 + CI | 通过率 ≥ 90%、CI 全绿 |
| 8 | 第 9 篇 §9：前后端分离 + Ubuntu 部署 | 可访问的产品 |

### 第 3 个月：进阶与输出

| 周 | 内容 | 产出 |
|---|---|---|
| 9 | 用 Cordis 手写最小 Agent 运行时（参考第 8 篇包地图） | mini-harness 仓库 |
| 10 | 写一个 MCP Server 接你熟悉的系统 | 可复用 MCP Server |
| 11 | 研究一个前沿方向（评测/安全/多 Agent）做小实验 | 实验报告 |
| 12 | 输出：技术博客 ×3 + 一个开源插件 | 简历作品集 |

## 6. 资源清单

- **DSH 官方仓库**：<https://github.com/deepseek-ai/dsh>（含 .agents/notes/ 设计文档）
- **Tritium 系列**：<https://www.tritium.work>（本系列多节整合来源，CC BY-NC-SA）
- **Cordis**：npm `@deepseek-ai/cordis`
- **MCP 规范**：<https://modelcontextprotocol.io>
- 课程：Andrej Karpathy LLM 系列、DeepLearning.AI Short Courses（Function Calling/Agentic RAG/Multi-Agent）
- 论文三篇奠基作（本科可读）：ReAct（arXiv:2210.03629）、Reflexion（arXiv:2303.11366）、Toolformer（arXiv:2302.04761）

## 7. 检验：读完本篇，你能回答吗

1. MCP 会话生命周期六个阶段是什么？三种传输怎么选？
2. 多 Agent 三种模式与成本铁律是什么？
3. 生产评测五个共识里哪个是 2026 踩坑最多的？
4. 三个月路线的三个里程碑分别是什么？

## 本章小结（全系列收官）

**框架会过时，思想不会。** 函数调用、工具循环、上下文管理、安全边界、评测门禁——这些底层思想不会变。而你现在拥有的是一套完整的搭建路线：从 LLM 的物理定律到 Harness 的九块积木，从 35 行循环到生产级产品。

把本系列读完、把第 9 篇的项目做完，你就不是"会用 Agent 的人"，而是"能造 Agent 的人"。

---

*全系列完。欢迎把学习过程写成博客，标注"基于 DSH 学习系列 + Tritium 搭建路线"即可自由引用。*