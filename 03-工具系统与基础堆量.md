# 第 3 篇 · 工具系统与基础堆量

> **搭建路线**：第 3 块积木——给 Agent 装上"手脚"：文件、Shell、Web 工具，以及让工具稳定工作的工程规范。没有这一步，后续的上下文引擎、记忆、Planning 都无东西可管。
> **本篇目标**：掌握工具契约的三层设计（每个都有代码）；真实世界工具的四件套规范；Provider 边界；以及"截断是第一道防线"。
> **难度**：⭐⭐⭐

---

## 1. 为什么先"堆量"再"管理"

第 2 篇的 Agent 只有 calculator 和 get_weather 两个教学工具，工具结果只有几行文本，几乎不会制造上下文压力。但真实世界的工具输出动辄几千行：

```text
read_file("src/main.py")        → 可能读到几千行源代码
execute_command("npm run build") → 可能吐出一整屏构建日志（含 stderr 与 exit code）
fetch_url("https://...")        → 可能抓回一篇很长的网页正文
工具失败时                    → stderr、exit code、timeout 信息也要进上下文
```

**先让 Agent 的手脚接触真实世界，上下文管理（第 4 篇）才有存在的意义**——这是"基础堆量"的核心逻辑：不是炫技，是为后续每个 Harness 模块提供真实的压力测试环境。

## 2. 工具契约的三层设计

工具是 Agent 与世界的唯一接口，所以**契约（模型看到的 schema + 返回结构）比实现更重要**。以最常用的 read_file 为例（这是生产级的完整形态）：

```ts
// 工具注册（以 DSH 的 ctx.tools.register 形态为例）
ctx.tools.register({
  name: 'read_file',
  description: '读取文本文件。大文件请用 offset/limit 分页读取。',  // ← 模型靠它决策
  parameters: {                        // 契约层 1：模型能传什么
    type: 'object', additionalProperties: false,
    properties: {
      path: { type: 'string', description: '文件绝对路径' },
      offset: { type: 'integer', description: '起始行（0 基），大文件分页用' },
      limit: { type: 'integer', default: 200, description: '最多返回行数' },
    },
    required: ['path'],
  },
  output: {                            // 契约层 2：模型能拿到什么（规范输出）
    schema: { type: 'object', additionalProperties: false,
      properties: {
        content: { type: 'string', required: true },
        truncated: { type: 'boolean', required: true },   // 明确告知截断
        totalLines: { type: 'integer', required: true },
      } },
  },
  async execute(args) { return readFileWithOffset(args) },   // 实现层：内部细节
})
```

**为什么三层设计**：
- **契约层 1（parameters）**决定"模型会不会乱传参"——`additionalProperties: false` 阻止发明字段，description 引导正确参数；
- **契约层 2（output）**决定"下游（前端/评测/CI）能否类型安全地消费"——规范输出让调用方不依赖具体实现；
- **实现层**可以随时重写（换解析库、换存储后端）而不影响任何调用方。

**错误契约**同样重要：失败时 `throw new Error("人类可读的原因")`——模型会读到错误消息并自我纠正；静默吞错是最差实践（第 2 篇 §1.3）。

## 3. 工具工程规范：真实世界的四件套

生产工具的标配（整合自 Tritium《Build An Agent From Scratch》[2.5]，CC BY-NC-SA）：

### 3.1 读文件支持 offset/limit 分页

```ts
// read_file 的分页实现（核心逻辑）
function readFileWithOffset(args: { path: string; offset?: number; limit?: number }) {
  const lines = readLines(args.path)              // 真实实现读全部行
  const start = args.offset ?? 0
  const count = args.limit ?? 200
  const sliced = lines.slice(start, start + count)
  return {
    content: sliced.join('\n'),
    truncated: start + count < lines.length,      // 显式告知还有更多
    totalLines: lines.length,
  }
}
// 描述里提醒模型：大文件要继续用 offset 读——让 Agent 自己管理读取粒度
```

### 3.2 Shell 工具强制 timeout + 杀进程树

```ts
// execute_command 的安全实现：超时必须杀整个进程树
const child = spawn('bash', [script, ...args], { cwd: SCRIPTS_ROOT })
const timer = setTimeout(() => {
  // 只 kill 父进程不够——子进程会残留继续跑
  child.kill('SIGKILL')        // 生产实现应递归杀进程组
}, timeoutSec * 1000)
// stdout/stderr 流式累积，超时标记 timedOut
```

> 为什么杀进程树：shell 脚本可能派生子进程（管道、后台任务），只杀父进程会留下孤儿进程继续消耗资源。

### 3.3 所有工具输出做截断（第一道防线）

```ts
const MAX_OUTPUT = 4000
const output = rawOutput.slice(-MAX_OUTPUT)   // 保留尾部（结论/错误码通常在后）
// 截断必须显式标记，不能静默丢数据：
return { output, truncated: rawOutput.length > MAX_OUTPUT }
```

> 截断是上下文的第一道防线——在上下文引擎（第 4 篇）之前就控制体积。**显式标记 truncated** 让模型知道"还有更多，需要时再读"。

### 3.4 描述即文档

工具描述写清"什么时候用、参数什么含义、大输出的处理方式"——描述是模型对工具唯一的认知来源（第 2 篇 §1.4）。

## 4. 安全三铁律：Shell 工具的事故防线

对科研 Agent 来说，最危险的工具是"能运行实验脚本"的工具。三条铁律，每条对应一个真实事故场景：

```ts
// 铁律 1：路径必须解析后仍落在白名单根内（防 ../ 逃逸 → 防任意命令执行）
if (relative(SCRIPTS_ROOT, full).startsWith('..')) throw new Error('脚本必须在白名单目录下')

// 铁律 2：强制超时 + SIGKILL 进程树（防实验失控吃满 GPU / 写爆磁盘）
const timer = setTimeout(() => child.kill('SIGKILL'), timeoutSec * 1000)

// 铁律 3：日志全量落盘（审计 + 可复现："这台 Agent 跑过什么"可回答）
writeFileSync(logPath, Buffer.concat(chunks).toString('utf8'))
```

> 设计安全时先列事故清单（第 9 篇展开）：每个安全机制都对应一个"最坏会发生什么"。

## 5. Provider 边界：模型是插拔的，循环不是

多模型支持的正确形态（[2.5] 篇）：**Agent Loop 只依赖一个统一的 LLM Client 接口**；OpenAI Responses / Chat Completions / Anthropic 等具体适配各自实现该接口，独立承载鉴权、重试、超时、流式。

```ts
// 统一接口（Loop 只面对它）
export interface LlmClient {
  complete(request: LlmRequest): Promise<AssistantMessage>
}
export interface StreamingLlmClient extends LlmClient {
  stream(request: LlmRequest): AsyncIterable<LlmEvent>
}

// Provider 适配在边界层（示例：Anthropic 适配器开头）
export class AnthropicClient implements LlmClient {
  complete(request: LlmRequest) {
    // 把内部 LlmRequest 映射为 Anthropic Messages API 的 payload
    // 处理鉴权、重试、超时
  }
}
```

> 工程意义：模型供应商升级/切换时，只改适配层，循环与工具完全不动——这也是第 9 篇 AutoResearcher 选择"模型路由由宿主管理"的原因（DSH 的宿主组合负责模型路由）。

## 6. DSH 参照：注册表是工具系统的生产形态

你在 §2 手写的注册逻辑，DSH 的生产形态是**工具注册表**（dsh-tools，第 8 篇展开）：

- `ctx.tools.register(definition)`：**必须带规范输出契约**（output schema），缺失即注册失败——"契约是强制"而不是"建议"；
- `tools.restrict(filter)`：agent 作用域的 allow/deny 掩码——**可见性控制是工具系统的一部分**；
- 工具与 Provider 一样可插拔：预设决定注册哪些工具（第 8 篇的 agent.cordis.yml），"没有工具 = 做不了"是最强的范围控制；
- 生产级工具还有：超时配置、approval 联动（敏感工具触发审批）、审计事件（每次调用进事件流）。

> 对照你自己的实现：你手动维护 TOOLS 数组；生产级是注册表 + 契约强制 + 作用域可见性控制 + 审批/审计联动。

## 7. 练习与常见坑

> **练习 3.1**：给第 2 篇的 35 行循环加一个 read_file 工具（带 offset/limit 契约），让它完成"统计大文件行数"任务——观察它是否学会分页读取（描述里提醒了它）。
> **练习 3.2**：把 run_shell 的 timeout 从"无"改成"5 秒杀进程"，跑一个 `sleep 10` 命令验证。

**常见坑**：
1. **截断不标记**：静默丢数据，模型以为读完了整个文件；
2. **offset/limit 不用**：大文件一次性读完，直接把上下文塞爆（第 4 篇要救的就是你）；
3. **timeout 只杀父进程**：孤儿进程继续跑，资源泄漏；
4. **工具描述写"说明书"不写"决策信息"**：模型不知道什么时候该用它。

## 8. 检验：读完本篇，你能回答吗

1. 工具契约的三层设计是什么？为什么 output 契约"缺失即注册失败"？
2. 真实世界工具的四件套规范是什么？截断为什么必须显式标记？
3. 为什么 timeout 要杀整个进程树而不是只杀父进程？
4. Shell 工具的三条安全铁律是什么？各防什么事故？
5. Provider 边界的意义是什么？切换模型时哪里要动、哪里不动？
6. 为什么说"没有工具 = 做不了"是最强控制？

（答案都在上文：1→§2，2→§3，3→§3.2，4→§4，5→§5，6→§6）

## 本章小结

第 3 篇给 Agent 装上了手脚：文件可读、命令可跑、网页可搜，且有契约、安全、边界规范兜底。但手脚一多，上下文压力就来了——工具输出、日志、错误开始累积。下一篇拼上"大脑的仪表盘"：上下文引擎。

**下一篇**：[第 4 篇 · 上下文引擎](04-上下文引擎.md) —— 历史与视图分离、六层流水线、KV 缓存工程化。
